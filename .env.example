DATABASE_URL=postgresql+psycopg://class_catchup:change-me@localhost:5432/class_catchup
APP_ENVIRONMENT=development
COOKIE_SECURE=false
SESSION_HOURS=12
DEFAULT_TIMEZONE=Africa/Lagos

# Required only when running the synthetic seed command.
DEMO_TEACHER_PASSWORD=
DEMO_STUDENT_PASSWORD=

# Required for a live model-driven Strands run. Confirm account availability first.
AWS_REGION=
BEDROCK_MODEL_ID=
