# Build Provenance

Prepared: 2026-09-12

## Starting material

- `implementation-plan.md` was supplied in the workspace.
- No application source, repository metadata, or repository-specific instruction file was present when implementation began.

## Newly created

- FastAPI/SQLAlchemy backend, migrations, authentication, worker, APIs, tests, and documentation.
- React/TypeScript/Vite web client.
- Territory-aware teacher onboarding and student-visible teacher introduction.
- Synthetic fractions PDF and its deterministic generator.
- Synthetic school, teacher, six learner records, timetable, mappings, occurrences, and fixture packet seed.
- Runtime proof JSON, evaluation matrix, architecture, setup, limitations, and demo script.

## Generated scaffolding

- The initial web skeleton was generated with `create-vite` and then its starter UI/assets were replaced or removed.
- `uv.lock` was generated by `uv`.
- `bun.lock` was generated by Bun.
- Alembic revision files were authored for this project rather than copied from another application.

## Dependencies and licenses

- Strands Agents SDK: Apache-2.0.
- FastAPI, SQLAlchemy, Alembic, Pydantic, psycopg, pwdlib, pypdf, pytest, React, Vite, TypeScript, and their transitive dependencies retain their respective upstream licenses.
- The source tree does not vendor dependency source; `.venv`, `node_modules`, and build output are ignored.

## Sample material

`fixtures/fractions-source.pdf` is generated from original synthetic text in `services/api/scripts/generate_fraction_fixture.py`. It contains no third-party textbook pages and no real student information.

## AI/provider provenance

- `artifacts/runtime-spike/strands-tool-call.json` is a real local Strands SDK custom-tool execution over a synthetic excerpt.
- No model-provider call was made because no provider credentials/model configuration were available.
- The seeded review packet has no `generation_run_id` and is labeled `fixture_or_teacher_edit` in the API/UI.
